it('expects method to determine if input is a palindrome', is_palindrome('mom') {
  expect(is_palindrome('mom')).toBe(true);
});
it('expects method to determine if input is a palindrome', is_palindrome('abba') {
  expect(is_palindrome('abba')).toBe(true);
});
it('expects method to determine if input is a palindrome', is_palindrome('beereb') {
  expect(is_palindrome('beereb')).toBe(false);;
});
it('expects method to determine if input is a palindrome', is_palindrome('hello olleh') {
  expect(is_palindrome('hello olleh')).toBe(true);
});
it('expects method to determine if input is a palindrome', is_palindrome('good morning') {
  expect(is_palindrome('good morning')).toBe(false);
});
